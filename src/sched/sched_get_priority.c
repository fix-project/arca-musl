#include <sched.h>
#include "syscall.h"

int sched_get_priority_max(int policy)
{
  // TODO (arca): sched_get_priority_max
  return -1;
}

int sched_get_priority_min(int policy)
{
  // TODO (arca): sched_get_priority_min
  return -1;
}
