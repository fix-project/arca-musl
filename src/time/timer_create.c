#include <time.h>
#include <setjmp.h>
#include <limits.h>
#include "pthread_impl.h"
#include "atomic.h"

int timer_create(clockid_t clk, struct sigevent *restrict evp, timer_t *restrict res)
{
	// TODO (arca): timer_create
	return -1;
}
